<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_image"
        android:label="Zolpia"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainUserActivity"
            android:label="@string/title_activity_main_user"></activity>
        <activity android:name=".SetTime"><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".TimeActivity" />
        <activity android:name=".SleepActivity" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/Material">

        </activity>
        <activity android:name=".SplashActivity"></activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Material" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"></activity>
    </application>

</manifest>